import { CardAvatarLargeDirective } from './card-avatar-large.directive';

describe('CardAvatarLargeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardAvatarLargeDirective();
    expect(directive).toBeTruthy();
  });
});
